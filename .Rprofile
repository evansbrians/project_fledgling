setHook(
  packageEvent("tidyverse", "attach"), 
  function(...) {
    library(cli)
    library(dbplyr)
    library(DBI)
    library(devtools)
    library(dtplyr)
    library(duckplyr)
    library(glue)
    library(here)
    library(pak)
    library(rlang)
    library(usethis)
  }
)
